- [x] Implement robust Arduino → JSON moisture serial collector (`data_collector.py`)
- [x] Add `requirements.txt` with required libraries (`streamlit`, `pyserial`)
- [x] Update Streamlit `main.py` to read latest moisture from `data_collector.py` and display it live using `st.empty()` / timed reruns
- [ ] Verify Arduino disconnected handling (no crash; shows connection error/status)


